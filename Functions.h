#ifndef _FUNCTIONS_H_
#define _FUNCTIONS_H_

class Pattern;
class Node;

extern unsigned char Rock_Generator(Pattern* p, int Amount, Node* node);

extern unsigned char Tree_Generator(Pattern* p, int Amount, Node* node);

extern unsigned char Rikka_Generator(Pattern* p, int Amount, Node* node);

#endif